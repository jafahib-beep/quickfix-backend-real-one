# quickfix-backend
Minimal backend for QuickFix app.

Quick start:
1. `npm install`
2. Create a `.env` file with `OPENAI_API_KEY=sk-...`
3. `npm run dev` (development) or `npm run build && npm start` (production)
